package UI;

public class MedDeleteController<CheckBox> {
/*
    public Button btnCancel;
    public Spinner spnId;


    private MedicamentService medicamentService;

    public void setService(MedicamentService medicamentService) {
        this.medicamentService = medicamentService;
    }

    private IRepository<Medicament> repository;

    public void MedicamentService(IRepository<Medicament> repository) {
        this.repository = repository;
    }

    public void btnCancelClick(ActionEvent actionEvent) {
        Stage stage = (Stage) btnCancel.getScene().getWindow();
        stage.close();
    }

    public void btnMedDeleteClick(ActionEvent actionEvent){

            String id = String.valueOf(spnId.getValue());
            repository.remove(id);
            btnCancelClick(actionEvent);
        }
*/

}


